/** Bundle that backs embedded Tangent sessions started from tangle-ui. */
export const TANGENT_BUNDLE_ID = "tangle";

/** Fallback Tangent origin used when the user has not configured one. */
export const DEFAULT_TANGENT_BASE_URL = "http://localhost:5173";
